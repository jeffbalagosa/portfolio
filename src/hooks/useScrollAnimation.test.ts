import { describe, expect, it } from 'vitest'

describe('useScrollAnimation legacy placeholder', () => {
	it('keeps the suite valid while TSX tests live elsewhere', () => {
		expect(true).toBe(true)
	})
})
